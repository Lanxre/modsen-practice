export default class RegisterMeetUp {

    constructor({
        id,
        user_id,
        meet_id
    }){
        this.id = id;
        this.user_id = user_id;
        this.meet_id = meet_id;
    }
}